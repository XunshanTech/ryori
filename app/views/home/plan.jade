doctype html
html
  head
    meta(charset='utf-8')
    meta(http-equiv='X-UA-Compatible', content='IE=edge')
    meta(name='viewport', content='width=1024')
    meta(name="apple-mobile-web-app-capable", content="yes")
    title 日料栈

    link(rel='stylesheet', href='/lib/impress.js/css/impress-demo.css')
    style(type='text/css').
      .slide img {
        width: 100%;
      }
  body(class="impress-not-supported")
    .fallback-message
      p Your browser
        b doesn't support the features required
        |  by impress.js, so you are presented with a simplified version of this presentation.
      p For the best experience please use the latest
        b Chrome,
        b Safari
        |  or
        b Firefox
        |  browser.
    .hint
      p 使用空格或方向键浏览

    #impress

      #bored.step.slide(data-x="0", data-y="-1500")
        img(src='/img/plan/01.jpg')
      - var index = 2;
        while index < 22
          - var dataY = 1000 * (index - 2) - 500;
          - var name = '/img/plan/' + (index < 10 ? '0' + index : index) + '.jpg';
            .step.slide(data-x="0", data-y=dataY)
              img(src=name)
          - index++;

    script.
      if ("ontouchstart" in document.documentElement) {
        document.querySelector(".hint").innerHTML = "<p>左右划动浏览</p>";
      }
    script(src='/lib/impress.js/js/impress.js')
    script.
      impress().init();